import store from 'store'

export function requireLogin(nextState, replaceState) {
  console.log("requireLogin");
  console.log(store.getState());
  if (!store.getState().auth) {
    replaceState({
      nextPathname: nextState.location.pathname
    }, '/auth/login')
  }
}