import React from 'react'

export default class Profile extends React.Component {
  render() {
    const { profile } = this.props;

    return (
      <div className="large-12 columns">
        {profile.email}
        <button type="button" onClick={() => this.handleLoginClick()}>Logout</button>
      </div>
    )
  }
}
