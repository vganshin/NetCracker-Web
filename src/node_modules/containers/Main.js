import React from 'react'
import { connect } from 'react-redux'
import Episode from 'components/Episode'
import getEpisodes from 'actions/episodesActions'
import getSubsEpisodes from 'actions/subsEpisodesActions'
import Feed from 'components/Feed'


@connect(state => state)
export default class Main extends React.Component {

    componentDidMount() {
        const { dispatch }  = this.props;
        //getEpisodes(10, null, false)(dispatch);
		getSubsEpisodes(10, null, true)(dispatch);
    }

    render() {
        var { episodes }  = this.props;
        return (

            <div>

                <div className="row">
                    <div className="large-6 large-centered columns">
                            <dl className="sub-nav">
                                <dt>Filter:</dt>
                                <dd className="active"><a href="#">All</a></dd>
                                <dd><a href="#">Subscribes</a></dd>
                                <dd><a href="#">Favourite</a></dd>
                            </dl>
                    </div>
                </div>
                <Feed episodes={episodes}/>
            </div>
        )
    }
} 